const EMAIL = "ayxwdcx@163.com";

const translations = {
    zh: {
        brand: "毛玉林",
        navAbout: "关于",
        navSkills: "技能",
        navProjects: "项目",
        navLearning: "学习",
        navContact: "联系",
        themeButton: "Dark",
        themeButtonApple: "Light",
        heroEyebrow: "Nankai University · Software Engineering",
        name: "毛玉林",
        heroSubtitle: "南开大学软件工程 2026 届本科生，研0准研究生。关注算法可靠性、工程系统与智能应用落地，偏好把复杂问题拆成可验证、可维护的产品能力。",
        copyEmail: "Email",
        copied: "邮箱已复制",
        copyFailed: "复制失败，请手动复制 ayxwdcx@163.com",
        avatarAlt: "毛玉林头像",
        panelLabel: "Current Focus",
        panelTitle: "算法工程 / 软件开发实习",
        panelText: "具备科研项目、企业实践和独立开发经验，能够在模型、后端与交互原型之间建立清晰的工程闭环。",
        metricOneValue: "2029",
        metricOneLabel: "南开大学软件工程",
        metricTwoValue: "DTA",
        metricTwoLabel: "科研方向",
        metricThreeValue: "Full-stack",
        metricThreeLabel: "工程实践",
        aboutEyebrow: "About",
        aboutTitle: "以工程确定性处理真实问题，以研究视角保持技术深度。",
        eduTitle: "教育经历",
        eduText: "南开大学软件工程专业 2026 届本科生，已获得保研本院资格，目前以研0身份继续积累科研与工程能力。",
        foundationTitle: "专业基础",
        foundationText: "专业排名前 30%，系统学习 C++、数据结构、数据库、Java 等课程，重视复杂度分析、接口边界与可维护性。",
        practiceTitle: "实践方向",
        practiceText: "参与国家级大创、企业实践与独立项目开发，能够从需求梳理推进到原型、实现、验证与展示。",
        labTitle: "实验室研究",
        labText: "围绕基于图神经网络的药物-靶标相互作用预测开展研究，关注分子与蛋白表示学习、表征融合、跨模态对齐和可解释性方法。",
        labMeta: "方向：图神经网络 · 药物-靶标相互作用 · 表征学习",
        skillsEyebrow: "Skills",
        skillsTitle: "覆盖算法实验、后端服务、前端原型与部署协作的完整开发链路。",
        projectsEyebrow: "Selected Work",
        projectsTitle: "精选项目",
        projectOneMeta: "PyTorch · Web Platform",
        projectOneTitle: "推荐算法投毒攻击研究",
        projectOneText: "国家级大创项目，集成多种推荐算法，构建投毒攻击模拟、指标评估与可视化分析流程。",
        projectTwoMeta: "Vue · LangChain · LLM API",
        projectTwoTitle: "LangChain 智能客服系统",
        projectTwoText: "中软国际企业实践项目，面向多场景服务咨询，整合 LLM API、语音交互与地图能力。",
        projectThreeMeta: "C++ · QT",
        projectThreeTitle: "Ash Impact 横板射击游戏",
        projectThreeText: "独立实现角色操控、碰撞检测与关卡机制，完成从玩法原型到校级展示的完整交付。",
        moreProjectsClosed: "展开更多项目",
        moreProjectsOpen: "收起更多项目",
        projectFourMeta: "HTML/CSS · Interaction Design",
        projectFourTitle: "终末星界秘所",
        projectFourText: "面向兴趣内容的独立站点，探索轻量交互、视觉表达与内容组织。",
        projectFiveMeta: "GNN · Bioinformatics",
        projectFiveTitle: "药物-靶标预测研究",
        projectFiveText: "围绕图神经网络、表征融合与可解释性方法，参与实验室科研方向建设。",
        projectSixMeta: "Engineering · Prototypes",
        projectSixTitle: "工程原型合集",
        projectSixText: "沉淀算法练习、工程实验与智能应用原型，持续补全可复用的开发经验。",
        projectSevenMeta: "HarmonyOS NEXT · ArkTS · SQLite",
        projectSevenTitle: "谷仓 · 二次元收藏图鉴",
        projectSevenText: "离线优先的原生收藏管理 App，支持藏品录入、分类筛选、角色图鉴、数据统计与本地图片存储。",
        learningEyebrow: "Learning",
        learningTitle: "近期技术关注",
        learningOneTitle: "大模型工程与检索增强",
        learningOneText: "关注 LangChain、Prompt 设计与 RAG 应用，把模型能力组织成稳定、可评估的服务流程。",
        learningTwoTitle: "云原生部署与微服务",
        learningTwoText: "学习 Kubernetes、Docker 和 CI/CD，关注服务部署、环境一致性与持续交付效率。",
        learningThreeTitle: "算法性能与系统优化",
        learningThreeText: "持续练习算法题与性能分析，在复杂系统中寻找更清晰、更高效的实现路径。",
        labEyebrow: "实验室",
        contactEyebrow: "Contact",
        contactTitle: "保持联系",
        contactText: "欢迎交流算法工程、软件开发、智能应用与科研合作相关机会。",
        footerLink: "终末星界秘所"
    },
    en: {
        brand: "Yuri Mao",
        navAbout: "About",
        navSkills: "Skills",
        navProjects: "Work",
        navLearning: "Learning",
        navContact: "Contact",
        themeButton: "Dark",
        themeButtonApple: "Light",
        heroEyebrow: "Nankai University · Software Engineering",
        name: "Yuri Mao",
        heroSubtitle: "Software Engineering undergraduate at Nankai University, class of 2026, and incoming graduate student focused on reliable algorithms, engineering systems, and intelligent applications that can be shipped and evaluated.",
        copyEmail: "Copy email",
        copied: "Email copied",
        copyFailed: "Copy failed. Please copy ayxwdcx@163.com manually.",
        avatarAlt: "Portrait of Yuri Mao",
        panelLabel: "Current Focus",
        panelTitle: "Algorithm Engineering / Software Internship",
        panelText: "Research, enterprise practice, and independent development experience, connecting models, backend systems, and interaction prototypes into clear engineering loops.",
        metricOneValue: "2029",
        metricOneLabel: "Nankai SE",
        metricTwoValue: "DTA",
        metricTwoLabel: "Research focus",
        metricThreeValue: "Full-stack",
        metricThreeLabel: "Engineering practice",
        aboutEyebrow: "About",
        aboutTitle: "Engineering discipline for real problems, with research depth where it matters.",
        eduTitle: "Education",
        eduText: "Software Engineering undergraduate at Nankai University, class of 2026, admitted to continue graduate study in the same school while building research and engineering depth.",
        foundationTitle: "Foundation",
        foundationText: "Top 30% in the major, with coursework in C++, data structures, databases, and Java, and attention to complexity, boundaries, and maintainability.",
        practiceTitle: "Practice",
        practiceText: "Experience across national innovation projects, enterprise practice, and independent builds, moving from requirements to prototypes, implementation, validation, and presentation.",
        skillsEyebrow: "Skills",
        skillsTitle: "A development path spanning algorithm experiments, backend services, frontend prototypes, and deployment collaboration.",
        projectsEyebrow: "Selected Work",
        projectsTitle: "Selected Projects",
        projectOneMeta: "PyTorch · Web Platform",
        projectOneTitle: "Recommendation Poisoning Attack Research",
        projectOneText: "National innovation project integrating recommendation algorithms with poisoning simulation, metric evaluation, and visual analysis.",
        projectTwoMeta: "Vue · LangChain · LLM API",
        projectTwoTitle: "LangChain Intelligent Customer Service",
        projectTwoText: "Enterprise practice project for multi-scenario service consultation, integrating LLM APIs, voice interaction, and map capabilities.",
        projectThreeMeta: "C++ · QT",
        projectThreeTitle: "Ash Impact Side-scrolling Shooter",
        projectThreeText: "Independently built character control, collision detection, and level mechanics, taking the game from prototype to university showcase.",
        moreProjectsClosed: "Show more projects",
        moreProjectsOpen: "Show fewer projects",
        projectFourMeta: "HTML/CSS · Interaction Design",
        projectFourTitle: "Endfield Secret Base",
        projectFourText: "An independent interest site exploring lightweight interaction, visual expression, and content structure.",
        projectFiveMeta: "GNN · Bioinformatics",
        projectFiveTitle: "Drug-target Prediction Research",
        projectFiveText: "Lab research around graph neural networks, representation fusion, and explainable prediction methods.",
        projectSixMeta: "Engineering · Prototypes",
        projectSixTitle: "Engineering Prototype Set",
        projectSixText: "A growing collection of algorithm practice, engineering experiments, and intelligent application prototypes.",
        projectSevenMeta: "HarmonyOS NEXT · ArkTS · SQLite",
        projectSevenTitle: "Gucang · Anime Collection Archive",
        projectSevenText: "An offline-first native collection manager with item cataloging, filters, character galleries, statistics, and local image storage.",
        labTitle: "Lab Research",
        labText: "Research around graph neural network-based drug-target interaction prediction, focusing on molecular and protein representation learning, embedding fusion, cross-modal alignment, and explainability.",
        labMeta: "Focus: Graph neural networks · Drug-target interaction · Representation learning",
        learningEyebrow: "Learning",
        learningTitle: "Current technical focus",
        learningOneTitle: "LLM engineering and retrieval augmentation",
        learningOneText: "Exploring LangChain, prompt design, and RAG applications to organize model capabilities into stable, evaluable service flows.",
        learningTwoTitle: "Cloud-native deployment and microservices",
        learningTwoText: "Studying Kubernetes, Docker, and CI/CD with attention to deployment reliability, environment consistency, and delivery efficiency.",
        learningThreeTitle: "Algorithm performance and systems optimization",
        learningThreeText: "Practicing algorithm problems and performance analysis to find clearer, more efficient implementations in complex systems.",
        labEyebrow: "Lab",
        contactEyebrow: "Contact",
        contactTitle: "Get in touch",
        contactText: "Reach me for algorithm engineering, software development, intelligent applications, and research collaboration.",
        footerLink: "Endfield Secret Base"
    }
};

const header = document.querySelector("[data-header]");
const toast = document.querySelector("[data-toast]");
const themeToggle = document.querySelector("[data-theme-toggle]");
const langToggle = document.querySelector("[data-lang-toggle]");
const copyEmailButtons = document.querySelectorAll("[data-copy-email]");
const projectMore = document.querySelector("[data-project-more]");
const projectToggle = document.querySelector("[data-project-toggle]");
const revealTargets = document.querySelectorAll(".reveal");
const interactiveSurfaces = document.querySelectorAll(".hero-visual, .feature-card, .project-card, .lab-inner, .contact-inner");
const prefersReducedMotion = window.matchMedia("(prefers-reduced-motion: reduce)");

let currentLang = localStorage.getItem("lang") || "zh";
let currentTheme = localStorage.getItem("theme") || "apple";
let toastTimer = null;

const getText = (key) => translations[currentLang][key] || translations.zh[key] || "";

const syncProjectToggle = () => {
    if (!projectMore || !projectToggle) return;
    const isOpen = projectMore.classList.contains("is-open");
    const label = projectToggle.querySelector("span");
    if (label) label.textContent = getText(isOpen ? "moreProjectsOpen" : "moreProjectsClosed");
    projectToggle.setAttribute("aria-expanded", String(isOpen));
};

const syncPointerPosition = (event) => {
    if (prefersReducedMotion.matches) return;
    const target = event.currentTarget;
    const rect = target.getBoundingClientRect();
    target.style.setProperty("--pointer-x", ((event.clientX - rect.left) / rect.width).toFixed(3));
    target.style.setProperty("--pointer-y", ((event.clientY - rect.top) / rect.height).toFixed(3));
};

const resetPointerPosition = (event) => {
    event.currentTarget.style.setProperty("--pointer-x", "0.5");
    event.currentTarget.style.setProperty("--pointer-y", "0.5");
};

const revealVisibleTargets = () => {
    const viewportHeight = window.innerHeight || document.documentElement.clientHeight;
    revealTargets.forEach((target) => {
        const rect = target.getBoundingClientRect();
        if (rect.top < viewportHeight * 0.92 && rect.bottom > 0) {
            target.classList.add("is-visible");
        }
    });
};

const showToast = (message) => {
    if (!toast) return;
    toast.textContent = message;
    toast.classList.add("is-visible");
    clearTimeout(toastTimer);
    toastTimer = setTimeout(() => {
        toast.classList.remove("is-visible");
    }, 1800);
};

const fallbackCopy = (text) => {
    const textarea = document.createElement("textarea");
    textarea.value = text;
    textarea.setAttribute("readonly", "");
    textarea.style.position = "fixed";
    textarea.style.left = "-9999px";
    document.body.appendChild(textarea);
    textarea.select();
    const success = document.execCommand("copy");
    textarea.remove();
    return success;
};

const copyEmail = async () => {
    try {
        if (navigator.clipboard?.writeText) {
            await navigator.clipboard.writeText(EMAIL);
        } else if (!fallbackCopy(EMAIL)) {
            throw new Error("copy failed");
        }
        showToast(getText("copied"));
    } catch {
        if (fallbackCopy(EMAIL)) {
            showToast(getText("copied"));
        } else {
            showToast(getText("copyFailed"));
        }
    }
};

const applyLanguage = () => {
    document.documentElement.lang = currentLang === "zh" ? "zh-CN" : "en";

    document.querySelectorAll("[data-i18n]").forEach((element) => {
        const key = element.dataset.i18n;
        element.textContent = getText(key);
    });

    document.querySelectorAll("[data-i18n-attr]").forEach((element) => {
        element.dataset.i18nAttr.split(",").forEach((pair) => {
            const [attr, key] = pair.split(":").map((part) => part.trim());
            if (attr && key) element.setAttribute(attr, getText(key));
        });
    });

    if (langToggle) {
        langToggle.textContent = currentLang === "zh" ? "EN" : "中";
        langToggle.setAttribute("aria-label", currentLang === "zh" ? "Switch to English" : "切换到中文");
    }

    syncProjectToggle();
    localStorage.setItem("lang", currentLang);
};

const applyTheme = () => {
    document.body.dataset.theme = currentTheme;
    if (themeToggle) {
        themeToggle.textContent = currentTheme === "apple" ? getText("themeButton") : getText("themeButtonApple");
        themeToggle.setAttribute("aria-label", currentTheme === "apple" ? "切换到舞台紫主题" : "切换到苹果风主题");
    }
    localStorage.setItem("theme", currentTheme);
};

const updateHeader = () => {
    header?.classList.toggle("is-scrolled", window.scrollY > 8);
};

themeToggle?.addEventListener("click", () => {
    currentTheme = currentTheme === "apple" ? "stage" : "apple";
    applyTheme();
});

/* Background particles canvas */
let bgCanvas = null;
let bgCtx = null;
let particles = [];
let particleAnimId = null;
let time = 0;
let sparkleTimer = 0;

function createParticlesCanvas() {
    if (bgCanvas) return;
    bgCanvas = document.getElementById("bg-canvas");
    if (!bgCanvas) return;
    bgCtx = bgCanvas.getContext("2d");
    resizeCanvas();
    window.addEventListener("resize", resizeCanvas);
}

function resizeCanvas() {
    if (!bgCanvas) return;
    bgCanvas.width = Math.max(document.documentElement.clientWidth, window.innerWidth || 0);
    bgCanvas.height = Math.max(document.documentElement.clientHeight, window.innerHeight || 0);
}

function rand(min, max) { return Math.random() * (max - min) + min; }

function initParticles(count = 40) {
    particles = [];
    const w = bgCanvas.width;
    const h = bgCanvas.height;
    const themeIsStage = document.body.dataset.theme === "stage";
    const baseColor = getComputedStyle(document.documentElement).getPropertyValue(themeIsStage ? "--accent" : "--accent") || "#0071e3";
    for (let i = 0; i < count; i++) {
        if (themeIsStage) {
            particles.push({
                x: rand(0, w),
                y: rand(0, h),
                r: rand(2.6, 7.5),
                vx: rand(-0.1, 0.1),
                vy: rand(-0.04, 0.04),
                alpha: rand(0.12, 0.36),
                hueShift: rand(-30, 30)
            });
        } else {
            particles.push({
                x: rand(0, w),
                y: rand(0, h),
                r: rand(2.4, 7),
                vx: rand(-0.12, 0.12),
                vy: rand(-0.05, 0.05),
                alpha: rand(0.16, 0.4),
                hueShift: rand(-20, 20)
            });
        }
    }
}

function drawParticles() {
    if (!bgCtx) return;
    const w = bgCanvas.width;
    const h = bgCanvas.height;
    bgCtx.clearRect(0, 0, w, h);
        const themeIsStage = document.body.dataset.theme === "stage";
    drawGradientBand(w, h, time, themeIsStage);
    for (const p of particles) {
        p.x += p.vx;
        p.y += p.vy;
        if (p.x < -100) p.x = w + 100;
        if (p.x > w + 100) p.x = -100;
        if (p.y < -100) p.y = h + 100;
        if (p.y > h + 100) p.y = -100;

        const grd = bgCtx.createRadialGradient(p.x, p.y, 0, p.x, p.y, p.r);
        if (themeIsStage) {
            grd.addColorStop(0, `rgba(255,121,198,${p.alpha})`);
            grd.addColorStop(0.58, `rgba(176,127,255,${p.alpha * 0.28})`);
            grd.addColorStop(1, 'rgba(0,0,0,0)');
        } else {
            grd.addColorStop(0, `rgba(0,113,227,${p.alpha})`);
            grd.addColorStop(0.58, `rgba(99,179,255,${p.alpha * 0.24})`);
            grd.addColorStop(1, 'rgba(0,0,0,0)');
        }

        bgCtx.globalCompositeOperation = 'screen';
        bgCtx.fillStyle = grd;
        bgCtx.beginPath();
        bgCtx.arc(p.x, p.y, p.r, 0, Math.PI * 2);
        bgCtx.fill();
    }

    // draw connecting lines for a subtle network effect
    drawConnections(bgCtx, particles, themeIsStage);

    sparkleTimer += 1;
    if (sparkleTimer > 26) {
        drawSparkles(bgCtx, w, h, themeIsStage);
        sparkleTimer = 0;
    }
}

function drawGradientBand(w, h, t, themeIsStage) {
    // slow-moving, very soft band to add depth
    const grd = bgCtx.createLinearGradient(0, h * (0.2 + Math.sin(t * 0.0006) * 0.06), w, h * (0.8 + Math.cos(t * 0.0005) * 0.06));
    if (themeIsStage) {
        grd.addColorStop(0, 'rgba(18,9,40,0.0)');
        grd.addColorStop(0.28, 'rgba(176,127,255,0.055)');
        grd.addColorStop(0.6, 'rgba(255,121,198,0.045)');
        grd.addColorStop(1, 'rgba(10,4,20,0.0)');
    } else {
        grd.addColorStop(0, 'rgba(255,255,255,0.0)');
        grd.addColorStop(0.18, 'rgba(0,113,227,0.08)');
        grd.addColorStop(0.6, 'rgba(99,179,255,0.05)');
        grd.addColorStop(1, 'rgba(255,255,255,0.0)');
    }
    bgCtx.save();
    bgCtx.globalCompositeOperation = 'overlay';
    bgCtx.fillStyle = grd;
    bgCtx.fillRect(-50, -50, w + 100, h + 100);
    bgCtx.restore();
}

function drawConnections(ctx, nodes, themeIsStage) {
    const maxDist = 128;
    ctx.save();
    ctx.lineWidth = 0.9;
    for (let i = 0; i < nodes.length; i++) {
        const a = nodes[i];
        for (let j = i + 1; j < nodes.length; j++) {
            const b = nodes[j];
            const dx = a.x - b.x;
            const dy = a.y - b.y;
            const d = Math.sqrt(dx * dx + dy * dy);
            if (d < maxDist) {
                const alpha = 1 - d / maxDist;
                if (themeIsStage) ctx.strokeStyle = `rgba(255,121,198,${0.1 * alpha})`;
                else ctx.strokeStyle = `rgba(0,113,227,${0.22 * alpha})`;
                ctx.beginPath();
                ctx.moveTo(a.x, a.y);
                ctx.lineTo(b.x, b.y);
                ctx.stroke();
            }
        }
    }
    ctx.restore();
}

function drawSparkles(ctx, w, h, themeIsStage) {
    let count = Math.max(1, Math.floor((w * h) / 1300000));
    ctx.save();
    for (let i = 0; i < count; i++) {
        const x = rand(0, w);
        const y = rand(0, h);
        const r = rand(1.1, 2.4);
        ctx.beginPath();
        if (themeIsStage) ctx.fillStyle = `rgba(255,217,251,${rand(0.08, 0.18)})`;
        else ctx.fillStyle = `rgba(150,200,255,${rand(0.12, 0.26)})`;
        ctx.arc(x, y, r, 0, Math.PI * 2);
        ctx.fill();
    }
    ctx.restore();
}

function animateParticles() {
    time += 16;
    drawParticles();
    particleAnimId = requestAnimationFrame(animateParticles);
}

function startBgParticles() {
    createParticlesCanvas();
    if (!bgCanvas) return;
    cancelAnimationFrame(particleAnimId);
    const areaFactor = Math.floor((bgCanvas.width * bgCanvas.height) / (1000 * 1000));
    const target = Math.min(42, Math.max(26, areaFactor + 28));
    initParticles(target);
    animateParticles();
}

function stopBgParticles() {
    if (particleAnimId) cancelAnimationFrame(particleAnimId);
}

function refreshParticlesForTheme() {
    if (!bgCanvas) return;
    bgCanvas.style.opacity = "";
    initParticles(particles.length || 40);
}

langToggle?.addEventListener("click", () => {
    currentLang = currentLang === "zh" ? "en" : "zh";
    applyLanguage();
    applyTheme();
});

copyEmailButtons.forEach((button) => {
    button.addEventListener("click", copyEmail);
});

interactiveSurfaces.forEach((surface) => {
    surface.addEventListener("pointermove", syncPointerPosition);
    surface.addEventListener("pointerleave", resetPointerPosition);
});

projectToggle?.addEventListener("click", () => {
    projectMore?.classList.toggle("is-open");
    syncProjectToggle();
});

if ("IntersectionObserver" in window) {
    const observer = new IntersectionObserver((entries) => {
        entries.forEach((entry) => {
            if (entry.isIntersecting) {
                entry.target.classList.add("is-visible");
                observer.unobserve(entry.target);
            }
        });
    }, { threshold: 0.16 });

    revealTargets.forEach((target) => observer.observe(target));
} else {
    revealTargets.forEach((target) => target.classList.add("is-visible"));
}

applyLanguage();
applyTheme();
updateHeader();
// init background particles
createParticlesCanvas();
startBgParticles();
requestAnimationFrame(revealVisibleTargets);
setTimeout(revealVisibleTargets, 240);
// when theme toggles, refresh particle colors and intensity
themeToggle?.addEventListener("click", () => {
    refreshParticlesForTheme();
});
window.addEventListener("scroll", updateHeader, { passive: true });
window.addEventListener("scroll", revealVisibleTargets, { passive: true });
window.addEventListener("resize", revealVisibleTargets);
